pipeline {
    agent {
        dockerfile true
    }
    
    stages {
       stage('Testing') {
            steps {
                sh 'mvn -version'
                sh 'mvn test'
            }
        }
    }
}
