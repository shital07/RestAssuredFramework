pipeline {
    agent {
        dockerfile true
    }
    
    stages {
       stage('Testing') {
            steps {
                sh 'cd /usr/apiautomation'
                sh 'pwd'
                sh 'mvn -version'
                sh 'mvn test'
            }
        }
    }
}
