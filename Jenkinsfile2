pipeline {
    agent {
        dockerfile true
    }
    
    stages {
       stage('Testing') {
            steps {
                sh 'pwd'
                sh 'mvn -version'
                sh 'mvn test'
            }
        }
    }
}
