pipeline {
    agent {
        dockerfile true
    }
    
    stages {
        stage('Testing') {
            steps {
                script {
                    def commandOutput = sh(script: 'mvn test 2>&1', returnStdout: true).trim()
                    println "Command output:\n${commandOutput}"
                    sh 'mvn test'
                }
            }
        }
    }
