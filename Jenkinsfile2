pipeline {
    agent {
        dockerfile true
    }
    
    stages {
       stage('Testing') {
            steps {
                sh 'cd'
                sh 'mvn -version'
                sh 'mvn test'
            }
        }
    }
}
